import '../../../core/viewmodels/base_view_model.dart';
import '../../../core/viewmodels/view_state.dart';

class PendingVisitViewModel extends BaseViewModel {
  // This ViewModel can be extended if needed for filtering or sorting logic
  // Currently, the screen uses StreamBuilder directly which is fine for MVVM
  // If we need to add client-side filtering or sorting, we can add it here
}

